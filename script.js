const header=document.querySelector('#header');const menuButton=document.querySelector('#menuButton');const nav=document.querySelector('#nav');const toast=document.querySelector('#toast');
window.addEventListener('scroll',()=>header.classList.toggle('scrolled',window.scrollY>25));
menuButton.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});
document.querySelectorAll('.nav a').forEach(link=>link.addEventListener('click',()=>{nav.classList.remove('open');menuButton.setAttribute('aria-expanded','false')}));
document.querySelectorAll('.copy-ip').forEach(button=>button.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(button.dataset.ip)}catch{const field=document.createElement('textarea');field.value=button.dataset.ip;document.body.append(field);field.select();document.execCommand('copy');field.remove()}toast.classList.add('show');clearTimeout(window.toastTimer);window.toastTimer=setTimeout(()=>toast.classList.remove('show'),2600)}));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
document.querySelector('#year').textContent=new Date().getFullYear();
async function loadServerStatus(){try{const response=await fetch('https://api.mcsrvstat.us/3/playflower.blackhost.pl');if(!response.ok)return;const data=await response.json();const status=document.querySelector('#statusText');const count=document.querySelector('#playerCount');if(data.online){status.textContent='ONLINE';count.textContent=data.players?`${data.players.online} GRACZY ONLINE`:'DOŁĄCZ DO NAS'}else{status.textContent='OFFLINE';status.style.color='#ff7186';count.textContent='SPRAWDŹ PÓŹNIEJ'}}catch{}}loadServerStatus();
