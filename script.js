const navToggle=document.querySelector("#navToggle"),nav=document.querySelector("#nav"),toast=document.querySelector("#toast");navToggle?.addEventListener("click",()=>nav.classList.toggle("open"));document.querySelectorAll(".nav a").forEach(e=>{e.addEventListener("click",()=>nav.classList.remove("open"))});function showToast(e){toast.textContent=e;toast.classList.add("show");window.setTimeout(()=>toast.classList.remove("show"),1800)}document.querySelectorAll("[data-copy]").forEach(e=>{e.addEventListener("click",async()=>{const t=e.getAttribute("data-copy");try{await navigator.clipboard.writeText(t);showToast("Skopiowano IP serwera!")}catch{showToast("Nie udało się skopiować. Skopiuj ręcznie.")}})});
